/*
** logger_channel.c for logger_channel in /home/guiho_r/depot/zappy/server/src/logging/logger
** 
** Made by ronan guiho
** Login   <guiho_r@epitech.net>
** 
** Started on  Fri May 31 21:08:12 2013 ronan guiho
** Last update Fri May 31 21:35:37 2013 ronan guiho
*/

#include <stdio.h>
#include <stdlib.h>

#include "logging/logger/logger.h"

void logging_logger_add_channel(t_logger *log, void *chan)
{
  if (log->channels)
    log->channels->push_back(log->channels, chan);
}

int logging_logger_delete_channel(t_logger *log, char *name)
{
  (void)log;
  (void)name;
  return (EXIT_SUCCESS);
}

void *logging_logger_get_channel(t_logger *log, char *name)
{
  (void)log;
  (void)name;
  return (NULL);
}
